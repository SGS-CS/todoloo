*button to open the date picker
    *not working on web (designed for phone), make web ver. have its own date picker
*add enter key to create tasks for convenience in testing
make it so when you press enter the textbox is reselected, so you don't click every time
*put dates and importance at the right of the task
option {
    make web date selector popup above button
    make web date selector popup beside button (x)
    make web date selector popup on top of button (x)
}
make "lists" into a separate function for organisation
    add an archive list
    add different tabs/projects for lists 
delete tasks with fadeout effect
option {
    add new tasks with a uniquely generated ID
    add new tasks with an ID incrementing by 1 each time (x)
}
